
namespace ICS.Mobile.Theme;
public enum Themes
{
    /// <summary>
    /// Sets the color mode to light - utilized when mode is default (unset)
    /// </summary>
    Light,

    /// <summary>
    /// Sets the color mode to dark
    /// </summary>
    Dark
}